## 📝 What changes



## 💡 Why



## 🧪 How I tested it

- [ ] `make check` passes locally
- [ ] Tested with Azure Blob Storage
- [ ] Tested on multisite (if relevant)

## 📸 Screenshots



## ✅ Checklist

- [ ] User-facing strings are wrapped in WordPress translation functions with the `diluxone-offload` text domain.
- [ ] User input is sanitized; output is escaped.
- [ ] No credentials, API keys, or secrets are logged.
- [ ] If this changes what a user sees, one bullet was added to the newest `= X.Y.Z =` entry of `readme.txt`, under its `Unreleased.` line (which stays: removing it is the release decision, see `docs/release.md`).
- [ ] Docs that describe what this PR changes are updated in this PR (`readme.txt`, `docs/`, `AGENTS.md`, `docs/architecture.md`).
